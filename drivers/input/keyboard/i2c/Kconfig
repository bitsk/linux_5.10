config KEYBOARD_I2C
    tristate "I2C Keyboard support"
    depends on I2C
    help
      Say Y here if you want to use an I2C connected keyboard.
      
      To compile this driver as a module, choose M here: the
      module will be called keyboard-i2c. 